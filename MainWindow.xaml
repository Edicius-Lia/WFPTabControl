<Window x:Class="WFPTabControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WFPTabControl"
        xmlns:vm="clr-namespace:WFPTabControl.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>
     <Menu DockPanel.Dock="Top">
         <MenuItem Header="文件">
             <MenuItem Header="Page1" Command="{Binding OpenPage1Command}" />
             <MenuItem Header="Page2" Command="{Binding OpenPage2Command}" />
         </MenuItem>
     </Menu>

     <TabControl ItemsSource="{Binding Tabs}" 
                 SelectedItem="{Binding SelectedTab}"
                 BorderThickness="0"
                 Background="Transparent">
         <TabControl.ItemTemplate>
             <DataTemplate>
                 <DockPanel>
                     <TextBlock Text="{Binding Header}" Margin="0,0,5,0"/>
                     <Button Content="×" 
                             Command="{Binding CloseCommand}"
                             DockPanel.Dock="Right"
                             Background="Transparent"
                             BorderThickness="0"/>
                 </DockPanel>
             </DataTemplate>
         </TabControl.ItemTemplate>
         <TabControl.ContentTemplate>
             <DataTemplate DataType="domain:TabItemViewModel">
                 <ContentPresenter Content="{Binding Content, Mode=TwoWay}" />
             </DataTemplate>
         </TabControl.ContentTemplate>
     </TabControl>
 </DockPanel>
</Window>

