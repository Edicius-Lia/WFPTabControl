<Window x:Class="WFPTabControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WFPTabControl"
        xmlns:vm="clr-namespace:WFPTabControl.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="文件">
                <MenuItem Header="新建标签1" Command="{Binding NewTabCommand}" CommandParameter="文档1"/>
                <MenuItem Header="新建标签2" Command="{Binding NewTabCommand}" CommandParameter="文档2"/>
            </MenuItem>
        </Menu>

        <TabControl ItemsSource="{Binding Tabs}" 
                    SelectedItem="{Binding SelectedTab}"
                    BorderThickness="0"
                    Background="Transparent">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock Text="{Binding Header}" Margin="0,0,5,0"/>
                        <Button Content="×" 
                                Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                DockPanel.Dock="Right"
                                Background="Transparent"
                                BorderThickness="0"/>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <TextBlock 
                                Text="{Binding Content}"
                                Margin="5"
                                TextWrapping="Wrap"
                                MaxHeight="300" />
                        </ScrollViewer>
                    </DockPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </DockPanel>
</Window>
